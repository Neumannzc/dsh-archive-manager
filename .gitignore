# dependencies
node_modules/

# build output
lib/
dist/
*.tsbuildinfo

# local package-manager stores / verification artifacts
.pnpm-store/
.npm-cache/
/.t.tgz
/package/
# Local-dev-only pnpm override (see the file's own header for the reason).
plugin/pnpm-workspace.yaml

# logs and temp
*.log
.DS_Store
